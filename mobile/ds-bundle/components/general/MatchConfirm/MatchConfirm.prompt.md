MatchConfirm from subeseguro-ds. Use via `window.SubeSeguroDS.MatchConfirm` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MatchConfirmProps {
  match: boolean;
  onMatch: (value: boolean) => void;
}
```
