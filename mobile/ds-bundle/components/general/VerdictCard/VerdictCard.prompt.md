VerdictCard from subeseguro-ds. Use via `window.SubeSeguroDS.VerdictCard` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface VerdictCardProps {
  verdict: "green" | "amber" | "red";
  /** Vehicle plate number */
  placa: string;
}
```
