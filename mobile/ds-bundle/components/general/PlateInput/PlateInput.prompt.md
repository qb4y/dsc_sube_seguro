PlateInput from subeseguro-ds. Use via `window.SubeSeguroDS.PlateInput` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PlateInputProps {
  value: string;
  onChange: (value: string) => void;
  onSubmit?: () => void;
  loading?: boolean;
  placeholder?: string;
}
```
